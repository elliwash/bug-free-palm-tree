# CLAUDE.md

## プロジェクト概要

このファイルはClaude Codeがプロジェクトを理解するためのガイドです。

## 開発ワークフロー

### 基本コマンド
- ビルド・テスト・Lintコマンドはここに記載する
- 新しい開発者が初回で `npm test` 等を成功できるよう、必要なセットアップ手順を明示すること

### タスク進め方
- 複雑なタスクは必ず**Planモード**から開始する
- PRは小さく保つ（目安: 変更行数 ~120行、1機能1PR）
- 常にスカッシュマージを使用してクリーンな線形履歴を維持する
- タスク完了時は少なくとも1時間ごとにコミットする

## コードスタイル

- 既存のパターンと一貫性を保つ
- 未使用コード・コメントアウトされたコードは残さない
- 部分的なフレームワーク移行は完結させる（中途半端な状態はモデルを混乱させる）

## Gitルール

- コミットメッセージは英語で命令形（例: `Add feature`, `Fix bug`）
- プッシュ前に必ずテストを実行する
- `.env` や認証情報を含むファイルは絶対にコミットしない

## Claude Code 使用ガイドライン

### コンテキスト管理
- コンテキスト使用率50%前後で `/compact` を実行する
- タスク切り替え時は `/clear` でセッションをリセットする
- 方向性がズレた場合は `Esc Esc`（`/rewind`）で戻る

### モデル選択
- 設計・計画フェーズ: Opus（精度優先）
- コーディングフェーズ: Sonnet（コスト・品質バランス）

### 指示の書き方
- ステップバイステップの手順より**ゴールと制約**を伝える
- `settings.json` で決定論的な振る舞いを強制する（`CLAUDE.md` の指示だけでは保証されない）

## セキュリティ

- `.env` / シークレットファイルへのアクセスは `permissions.deny` で制限する
- 不審なMCPサーバーは有効化しない
